# Web API base URL for frontend requests in browser mode.
# Leave empty to use same-origin (e.g. Vite proxy in dev or reverse-proxied backend in prod).
VITE_API_BASE_URL=

# Public Jaaz website base URL used for auth/billing external pages.
VITE_JAAZ_BASE_API_URL=https://jaaz.app
